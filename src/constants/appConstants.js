// Animation durations (in milliseconds)
export const ANIMATION_DURATIONS = {
  BUTTON_PULSE: 600,
  SPLASH_SCREEN: 4000,
  SCROLL_TRANSITION: 300,
};

// Z-index layers
export const Z_INDEX = {
  SPEED_CONTROL: 40,
  CACHE_INDICATOR: 50,
  MODAL: 50,
  SPLASH: 50,
};

// Virtual scrolling configuration
export const VIRTUAL_SCROLLING = {
  RENDER_BUFFER: 2,
};

// Gesture handling thresholds
export const GESTURE_CONFIG = {
  SWIPE_THRESHOLD: 50, // pixels
};

// Video playback configuration
export const VIDEO_CONFIG = {
  NORMAL_SPEED: 1.0,
  FAST_SPEED: 2.0,
};

// Cache configuration
export const CACHE_CONFIG = {
  DISPLAY_THRESHOLD_MIN: 0,
  DISPLAY_THRESHOLD_MAX: 100,
};

// Viewport offsets
export const VIEWPORT = {
  UNIT: 100, // vh
};
